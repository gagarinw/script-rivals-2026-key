-- [[ ILLIA SYSTEM V3 - UPDATED API URL ]] --

local API_URL = "https://script.google.com/macros/s/AKfycbzwHATow0sdUFFCfyIcBDsUCOD81dJVfSw7t8AJ6bKIrYrIoQ4YWjeDryCV9qpT-_8Egg/exec"
local HttpService = game:GetService("HttpService")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")

local canSave = (isfile and readfile and writefile and delfile)
local KEY_FILE = "illia_v3_key.txt"
local SETTINGS_FILE = "illia_v3_settings.json"

local Settings = {
    AimbotEnabled = true,
    AimRadius = 150,
    Sensitivity = 0.2, -- Установлено по вашему запросу для резкости
    AimKey = "T",      -- Клавиша активации изменена на T
    ShowFOV = true,
    ESP_Enabled = true,
    ESP_Rainbow = false,
    ESP_Hue = 0.6
}

local Colors = {
    MainBlue = Color3.fromRGB(0, 40, 100), -- Темно-синий
    AdminGold = Color3.fromRGB(255, 200, 0),
    BG = Color3.fromRGB(15, 15, 15),
    White = Color3.fromRGB(255, 255, 255),
    LogoutRed = Color3.fromRGB(200, 50, 50),
    SuccessGreen = Color3.fromRGB(40, 200, 40),
    Dark = Color3.fromRGB(25, 25, 25),
    PurpleBtn = Color3.fromRGB(128, 0, 128) -- Фиолетовые кнопки
}

local IsAdmin, ExpiryTimestamp = false, 0
local CurrentKey = ""
local HWID = game:GetService("RbxAnalyticsService"):GetClientId()

local function FormatTime(seconds)
    if seconds <= 0 then return "ИСТЕК" end
    local h = math.floor(seconds / 3600)
    local m = math.floor((seconds % 3600) / 60)
    local s = math.floor(seconds % 60)
    return string.format("%02d:%02d:%02d", h, m, s)
end

local function VerifyKey(key)
    local success, result = pcall(function()
        return game:HttpGet(API_URL .. "?check=" .. key .. "&hwid=" .. HWID)
    end)
    
    if success and result ~= "invalid" then
        CurrentKey = key
        if result == "admin" then
            IsAdmin = true
            if canSave then writefile(KEY_FILE, key) end
            return "ok"
        elseif result:find("valid") then
            local data = string.split(result, "|")
            ExpiryTimestamp = tonumber(data[2]) / 1000
            IsAdmin = false
            if canSave then writefile(KEY_FILE, key) end
            return "ok"
        end
    end
    return "invalid"
end

function BuildMenu()
    if CoreGui:FindFirstChild("IlliaV3_Menu") then CoreGui.IlliaV3_Menu:Destroy() end
    local G = Instance.new("ScreenGui", CoreGui); G.Name = "IlliaV3_Menu"
    
    local MainFrame = Instance.new("ScrollingFrame", G)
    MainFrame.Size = UDim2.new(0, 320, 0, 500)
    MainFrame.Position = UDim2.new(0.5, -160, 0.5, -250)
    MainFrame.BackgroundColor3 = Colors.BG
    MainFrame.BorderSizePixel = 0
    Instance.new("UICorner", MainFrame)
    
    local L = Instance.new("UIListLayout", MainFrame); L.Padding = UDim.new(0, 12); L.HorizontalAlignment = "Center"
    Instance.new("UIPadding", MainFrame).PaddingTop = UDim.new(0, 15)

    local Header = Instance.new("TextLabel", MainFrame)
    Header.Size = UDim2.new(1, 0, 0, 55)
    Header.Text = IsAdmin and "👑 ILLIA V3 ADMIN" or "👤 ILLIA V3 USER"
    Header.BackgroundColor3 = Colors.MainBlue -- Темно-синий фон заголовка
    Header.TextColor3 = Colors.White
    Header.Font = "GothamBold"
    Header.TextSize = 20
    Instance.new("UICorner", Header)

    local TimerLabel = Instance.new("TextLabel", MainFrame)
    TimerLabel.Size = UDim2.new(0.9, 0, 0, 30)
    TimerLabel.BackgroundTransparency = 1
    TimerLabel.TextColor3 = Colors.White
    TimerLabel.Font = "GothamBold"
    TimerLabel.TextSize = 14

    local LogoutBtn = Instance.new("TextButton", MainFrame)
    LogoutBtn.Size = UDim2.new(0.9, 0, 0, 40)
    LogoutBtn.BackgroundColor3 = Colors.PurpleBtn -- Фиолетовая кнопка
    LogoutBtn.Text = "ВЫЙТИ"
    LogoutBtn.TextColor3 = Colors.White
    LogoutBtn.Font = "GothamBold"
    Instance.new("UICorner", LogoutBtn)

    LogoutBtn.MouseButton1Click:Connect(function()
        if canSave then pcall(function() delfile(KEY_FILE) end) end
        G:Destroy()
        ShowLogin()
    end)

    RunService.RenderStepped:Connect(function()
        if not IsAdmin then
            local remaining = ExpiryTimestamp - os.time()
            TimerLabel.Text = "ОСТАЛОСЬ: " .. FormatTime(remaining)
            if remaining <= 0 then G:Destroy(); ShowLogin() end
        else
            TimerLabel.Text = "СТАТУС: БЕССРОЧНО"
        end
    end)
end

function ShowLogin()
    if CoreGui:FindFirstChild("IlliaLogin") then CoreGui.IlliaLogin:Destroy() end
    local A = Instance.new("ScreenGui", CoreGui); A.Name = "IlliaLogin"
    local B = Instance.new("Frame", A); B.Size = UDim2.new(0, 340, 0, 240); B.Position = UDim2.new(0.5,-170,0.5,-120); B.BackgroundColor3 = Colors.BG; Instance.new("UICorner", B)
    
    local Title = Instance.new("TextLabel", B)
    Title.Size = UDim2.new(1, 0, 0, 40)
    Title.Text = "Здраствуйте! Спасибо что выбрали мой чит"
    Title.BackgroundTransparency = 1
    Title.TextColor3 = Colors.MainBlue
    Title.Font = "GothamBold"
    Title.TextSize = 14

    local I = Instance.new("TextBox", B); I.Size = UDim2.new(0.8,0,0,50); I.Position = UDim2.new(0.1,0,0.3,0); I.PlaceholderText = "Введите ключ..."; I.BackgroundColor3 = Colors.Dark; I.TextColor3 = Colors.White; Instance.new("UICorner", I)
    local S = Instance.new("TextButton", B); S.Size = UDim2.new(0.8,0,0,50); S.Position = UDim2.new(0.1,0,0.65,0); S.BackgroundColor3 = Colors.PurpleBtn; S.Text = "ВОЙТИ"; S.TextColor3 = Colors.White; Instance.new("UICorner", S)
    
    S.MouseButton1Click:Connect(function()
        S.Text = "ПРОВЕРКА..."
        local res = VerifyKey(I.Text)
        if res == "ok" then
            S.Text = "УСПЕШНО!"
            task.wait(0.5); A:Destroy(); BuildMenu()
        else
            S.Text = "НЕВЕРНЫЙ КЛЮЧ"
            task.wait(1.5); S.Text = "ВОЙТИ"
        end
    end)
end

-- Авто-вход
if canSave and isfile(KEY_FILE) then
    local savedKey = readfile(KEY_FILE)
    if VerifyKey(savedKey) == "ok" then BuildMenu() else ShowLogin() end
else
    ShowLogin()
end
