-- [[ ILLIA SYSTEM V3 - TOGGLE EDITION ]] --

local API_URL = "https://script.google.com/macros/s/AKfycbx3NDT8ZQ7LEHDjRBhUmNtEJ3IdYlK4CRpnlix2u_nrkwJoWLfNA1f7ZwUWwBzySTNWzg/exec"
local HttpService = game:GetService("HttpService")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local HWID = game:GetService("RbxAnalyticsService"):GetClientId()

-- Проверка поддержки мыши
local movemouse = mousemoverel or (Input and (Input.mousemove or Input.movemouse))

-- Файлы
local canSave = (writefile and readfile and isfile)
local KEY_FILE = "illia_v3_key.txt"
local SETTINGS_FILE = "illia_v3_settings.json"

-- Настройки (У = KeyCode.E)
local Settings = {
    AimbotEnabled = false, -- По умолчанию выключен
    TeamCheck = true, 
    ESP_Enabled = true,
    ESP_Rainbow = false,
    ESP_Hue = 0.6,
    ESP_Speed = 1,
    ShowFOV = true,
    AimRadius = 150,
    Sensitivity = 0.3, 
    AimPart = "Head",
    MenuKey = Enum.KeyCode.Insert,
    ToggleKey = Enum.KeyCode.E -- Это кнопка "У" на клавиатуре
}

local Colors = {
    MainBlue = Color3.fromRGB(0, 100, 255),
    DarkBlue = Color3.fromRGB(0, 35, 100),
    Purple = Color3.fromRGB(120, 40, 180),
    Background = Color3.fromRGB(15, 15, 15),
    TitleBar = Color3.fromRGB(25, 25, 25)
}

local IsAdmin, ExpiryTimestamp = false, 0
local MainFrame = nil
local AimButtonLink = nil -- Ссылка для обновления текста кнопки

-- Функции данных
local function SaveData()
    if canSave then
        pcall(function() writefile(SETTINGS_FILE, HttpService:JSONEncode(Settings)) end)
    end
end

local function LoadData()
    if canSave and isfile(SETTINGS_FILE) then
        local s, data = pcall(function() return HttpService:JSONDecode(readfile(SETTINGS_FILE)) end)
        if s then for i, v in pairs(data) do Settings[i] = v end end
    end
end
LoadData()

-- Визуал FOV
local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 1.5
FOVCircle.Visible = false
FOVCircle.Transparency = 1
FOVCircle.Filled = false

local function IsEnemy(p)
    if not Settings.TeamCheck then return true end
    return p.Team ~= LocalPlayer.Team
end

-- Проверка ключа
local function VerifyKey(key)
    local url = API_URL .. "?check=" .. tostring(key) .. "&hwid=" .. HWID .. "&cb=" .. math.random(1, 99999)
    local s, r = pcall(function() return game:HttpGet(url) end)
    if s and r then
        local res = r:gsub("<[^>]*>", ""):gsub("%s+", "")
        if res == "admin" then 
            IsAdmin = true 
            if canSave then writefile(KEY_FILE, key) end
            return "ok"
        elseif res:find("valid|") then
            ExpiryTimestamp = tonumber(string.split(res, "|")[2]) / 1000
            if canSave then writefile(KEY_FILE, key) end
            return "ok"
        end
    end
    return "invalid"
end

-- Аим (Твоя логика из Rivals)
local function GetClosestTarget()
    local targetPos = nil
    local dist = Settings.AimRadius
    local mouseLoc = UserInputService:GetMouseLocation()

    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local char = p.Character
            local part = char:FindFirstChild(Settings.AimPart) or (char:FindFirstChild("Hitbox") and char.Hitbox:FindFirstChild(Settings.AimPart))
            local hum = char:FindFirstChildOfClass("Humanoid")

            if part and hum and hum.Health > 0 and IsEnemy(p) then
                local pos, vis = Camera:WorldToViewportPoint(part.Position)
                if vis then
                    local mDist = (Vector2.new(pos.X, pos.Y) - mouseLoc).Magnitude
                    if mDist < dist then
                        dist = mDist
                        targetPos = Vector2.new(pos.X, pos.Y)
                    end
                end
            end
        end
    end
    return targetPos
end

-- Цикл
RunService.RenderStepped:Connect(function()
    FOVCircle.Position = UserInputService:GetMouseLocation()
    FOVCircle.Radius = Settings.AimRadius
    FOVCircle.Visible = Settings.ShowFOV
    FOVCircle.Color = Settings.AimbotEnabled and Colors.MainBlue or Color3.new(1, 1, 1)

    -- ESP
    local espColor = Settings.ESP_Rainbow and Color3.fromHSV((tick() * (Settings.ESP_Speed * 0.1)) % 1, 0.8, 1) or Color3.fromHSV(Settings.ESP_Hue, 0.8, 1)
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local h = p.Character:FindFirstChild("ILLIA_CHAMS")
            if Settings.ESP_Enabled and IsEnemy(p) then
                if not h then h = Instance.new("Highlight", p.Character); h.Name = "ILLIA_CHAMS" end
                h.FillColor = espColor
            elseif h then h:Destroy() end
        end
    end

    -- Работа Аима (теперь только если Enabled == true)
    if Settings.AimbotEnabled and movemouse then
        local target = GetClosestTarget()
        if target then
            local mouseLoc = UserInputService:GetMouseLocation()
            local diff = (target - mouseLoc) * Settings.Sensitivity
            movemouse(diff.X, diff.Y)
        end
    end
end)

-- Меню
function BuildMenu()
    if CoreGui:FindFirstChild("IlliaMenuV3") then CoreGui.IlliaMenuV3:Destroy() end
    local MainGui = Instance.new("ScreenGui", CoreGui); MainGui.Name = "IlliaMenuV3"
    
    MainFrame = Instance.new("Frame", MainGui)
    MainFrame.BackgroundColor3 = Colors.Background; MainFrame.Size = UDim2.new(0, 280, 0, 500); MainFrame.Position = UDim2.new(0.5, -140, 0.5, -250)
    Instance.new("UICorner", MainFrame)

    local Title = Instance.new("TextLabel", MainFrame)
    Title.Size = UDim2.new(1, 0, 0, 50); Title.Text = "ILLIA V3"; Title.TextColor3 = Color3.new(1, 1, 1); Title.BackgroundColor3 = Colors.TitleBar; Title.Font = "GothamBold"; Title.TextSize = 22; Instance.new("UICorner", Title)

    local UIList = Instance.new("UIListLayout", MainFrame); UIList.Padding = UDim.new(0, 8); UIList.HorizontalAlignment = "Center"
    Instance.new("UIPadding", MainFrame).PaddingTop = UDim.new(0, 60)

    local function CreateButton(text, prop)
        local btn = Instance.new("TextButton", MainFrame)
        btn.Size = UDim2.new(0.9, 0, 0, 35); btn.Font = "GothamBold"; btn.TextColor3 = Color3.new(1, 1, 1)
        local function upd() 
            btn.Text = text .. ": " .. (Settings[prop] and "ON" or "OFF")
            btn.BackgroundColor3 = Settings[prop] and Colors.DarkBlue or Colors.Purple
        end
        btn.MouseButton1Click:Connect(function() Settings[prop] = not Settings[prop]; upd(); SaveData() end)
        if prop == "AimbotEnabled" then AimButtonLink = upd end -- Сохраняем ссылку для обновления
        upd(); Instance.new("UICorner", btn)
    end

    local function CreateSlider(text, prop, min, max)
        local f = Instance.new("Frame", MainFrame); f.Size = UDim2.new(0.9, 0, 0, 45); f.BackgroundTransparency = 1
        local l = Instance.new("TextLabel", f); l.Size = UDim2.new(1, 0, 0, 20); l.TextColor3 = Color3.new(1, 1, 1); l.TextSize = 11; l.Font = "GothamBold"; l.BackgroundTransparency = 1
        local b = Instance.new("Frame", f); b.Size = UDim2.new(1, 0, 0, 5); b.Position = UDim2.new(0, 0, 0, 30); b.BackgroundColor3 = Color3.fromRGB(40, 40, 40); Instance.new("UICorner", b)
        local fill = Instance.new("Frame", b); fill.BackgroundColor3 = Colors.MainBlue; Instance.new("UICorner", fill)
        local k = Instance.new("TextButton", b); k.Size = UDim2.new(0, 12, 0, 12); k.Text = ""; Instance.new("UICorner", k)
        
        local function upd(val)
            local p = (val - min) / (max - min)
            fill.Size = UDim2.new(p, 0, 1, 0); k.Position = UDim2.new(p, -6, 0.5, -6); l.Text = text .. ": " .. tostring(val)
        end
        local drag = false
        k.MouseButton1Down:Connect(function() drag = true end)
        UserInputService.InputEnded:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseButton1 then drag = false; SaveData() end end)
        UserInputService.InputChanged:Connect(function(i)
            if drag and i.UserInputType == Enum.UserInputType.MouseMovement then
                local p = math.clamp((i.Position.X - b.AbsolutePosition.X) / b.AbsoluteSize.X, 0, 1)
                local v = max <= 1 and math.round((min + (max - min) * p) * 100) / 100 or math.floor(min + (max - min) * p)
                Settings[prop] = v; upd(v)
            end
        end)
        upd(Settings[prop])
    end

    CreateButton("Aimbot (Key: У)", "AimbotEnabled")
    CreateButton("Team Check", "TeamCheck")
    CreateButton("ESP Master", "ESP_Enabled")
    CreateButton("ESP Rainbow", "ESP_Rainbow")
    CreateSlider("FOV Radius", "AimRadius", 50, 500)
    CreateSlider("Aim Speed", "Sensitivity", 0.05, 1)
end

function ShowLogin()
    local SG = Instance.new("ScreenGui", CoreGui)
    local F = Instance.new("Frame", SG); F.Size = UDim2.new(0, 300, 0, 160); F.Position = UDim2.new(0.5, -150, 0.5, -80); F.BackgroundColor3 = Colors.Background; Instance.new("UICorner", F)
    local T = Instance.new("TextBox", F); T.Size = UDim2.new(0.8, 0, 0, 40); T.Position = UDim2.new(0.1, 0, 0.2, 0); T.PlaceholderText = "Введите ключ..."; T.BackgroundColor3 = Color3.fromRGB(30, 30, 30); T.TextColor3 = Color3.new(1, 1, 1); Instance.new("UICorner", T)
    local B = Instance.new("TextButton", F); B.Size = UDim2.new(0.8, 0, 0, 40); B.Position = UDim2.new(0.1, 0, 0.6, 0); B.Text = "ВОЙТИ"; B.BackgroundColor3 = Colors.MainBlue; B.TextColor3 = Color3.new(1, 1, 1); Instance.new("UICorner", B)
    
    if canSave and isfile(KEY_FILE) then
        local k = readfile(KEY_FILE)
        if VerifyKey(k) == "ok" then SG:Destroy(); BuildMenu() return end
    end
    B.MouseButton1Click:Connect(function()
        if VerifyKey(T.Text) == "ok" then SG:Destroy(); BuildMenu() else B.Text = "ОШИБКА"; task.wait(1); B.Text = "ВОЙТИ" end
    end)
end

-- Обработка переключения на У (KeyCode.E)
UserInputService.InputBegan:Connect(function(i, g)
    if g then return end
    if i.KeyCode == Settings.ToggleKey then
        Settings.AimbotEnabled = not Settings.AimbotEnabled
        if AimButtonLink then AimButtonLink() end -- Обновляем кнопку в меню
        SaveData()
    end
    if i.KeyCode == Settings.MenuKey then if MainFrame then MainFrame.Parent.Enabled = not MainFrame.Parent.Enabled end end
end)

ShowLogin()
